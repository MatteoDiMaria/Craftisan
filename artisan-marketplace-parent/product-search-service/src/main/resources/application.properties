spring.data.mongodb.uri=mongodb://localhost:27017/product_db # Same DB as product-service

# Jersey configuration
spring.jersey.application-path=/api # Base path for all JAX-RS resources

# Server port (optional, defaults to 8080, ensure it's different from other services if run locally)
# server.port=8084

# Logging (optional)
# logging.level.org.springframework.web=DEBUG
# logging.level.org.springframework.data.mongodb.core.MongoTemplate=DEBUG
# logging.level.org.mongodb.driver=INFO

# Reminder: For text search to work, a text index needs to be created on the 'products' collection in MongoDB.
# Example: db.products.createIndex({ name: "text", description: "text", category: "text" })
